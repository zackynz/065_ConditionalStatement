#include <iostream>
using namespace std;

int main(){
    string status
    int nMatematika, nFisika;

    srand(time(0));
    nMatematika = rand() % 60;
    nFisika = rand() % 70%;

        if (nMatematika % 60 == 0){
        nBilangan = nBilangan+1;





}